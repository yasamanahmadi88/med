version: '3'
services:
  angular-app:
    build: .
    ports:
      - '9000:9000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: ['npm', 'run', 'start']
