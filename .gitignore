######################
# Node
######################
/node/
# No trailing slash: the agent harness links `node_modules` into each scratch worktree rather
# than copying it, and a trailing slash matches only a directory, so the symlink showed up as an
# untracked file in every one of them.
node_modules

######################
# Build / target / Angular
######################
/target/
/build/
/dist/
.angular/
coverage/
/out-tsc/

######################
# IDE
######################
.idea/
*.iml
.vscode/
*.swp

######################
# OS
######################
.DS_Store
Thumbs.db

######################
# Logs / temp
######################
*.log
/extracted/
*.tmp

######################
# Source archive (extracted into src/)
######################
/src-for-medPortal.rar

# Scratch worktrees the agent harness creates; each is a full checkout of this repo.
.claude/worktrees/
